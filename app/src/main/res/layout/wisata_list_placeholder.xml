<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="125dp"
    android:layout_marginBottom="@dimen/paddingVertical"
    android:orientation="vertical"
    app:cardCornerRadius="10dp"
    app:cardElevation="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#f5f5f5"
        android:orientation="horizontal">

        <View
            android:id="@+id/dashboard_wisata_image"
            android:layout_width="175dp"
            android:layout_height="match_parent"
            android:background="@color/background_placeholder"
            android:contentDescription="@string/app_name"
            android:scaleType="fitXY" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/paddingVertical">

            <View
                android:id="@+id/dashboard_wisata_name"
                android:layout_width="@dimen/text_caption_placeholder_width"
                android:layout_height="@dimen/view_placeholder_height"
                android:background="@color/background_placeholder" />

            <View
                android:id="@+id/dashboard_wisata_rating"
                android:layout_width="@dimen/ratingbar_placeholder_width"
                android:layout_height="@dimen/view_placeholder_height"
                android:layout_marginTop="@dimen/paddingVertical"
                android:background="@color/background_placeholder" />

            <View
                android:id="@+id/dashboard_wisata_description"
                android:layout_width="match_parent"
                android:layout_height="@dimen/view_placeholder_height"
                android:layout_marginTop="@dimen/paddingVertical"
                android:background="@color/background_placeholder" />
        </LinearLayout>
    </LinearLayout>

</androidx.cardview.widget.CardView>